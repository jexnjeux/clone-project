import { DefaultTheme } from 'styled-components';

const palette = {
  background: '#fff',
  black: '#111',
  grey: '#767676',
  lightGrey: '#d1d1d1',
  white: '#fff',
  red: '#f15151',
};

const font = {
  xs: '12px',
  sm: '16px',
  md: '24px',
  lg: '40px',
};

const theme: DefaultTheme = { palette, font };

export default theme;
